import { Routes } from '@angular/router';
import { JsonFormatterComponent } from './json-formatter.component';

export const jsonFormatterRoutes: Routes = [
  {
    path: '',
    component: JsonFormatterComponent
  }
];

