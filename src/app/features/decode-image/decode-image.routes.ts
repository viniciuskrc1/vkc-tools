import { Routes } from '@angular/router';
import { DecodeImageComponent } from './decode-image.component';

export const decodeImageRoutes: Routes = [
  {
    path: '',
    component: DecodeImageComponent
  }
];

